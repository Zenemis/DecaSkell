interface Drawable {
    void draw();
}

class Circle implements Drawable {
    void draw() {
        Print("Drawing a circle");
    }
}

void main() {
    Drawable shape = New(Circle);
    if (shape == null){
        return;
    }
    shape.draw(); // Affiche "Drawing a circle"
}